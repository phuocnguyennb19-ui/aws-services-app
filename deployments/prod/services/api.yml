# Backend API Configuration for Dev
service:
  enabled: true
  app_name: "backend-api"
  service_type: "api"
  cpu: 256
  memory: 512
  container_port: 8080
  min_capacity: 1
  max_capacity: 2
  health_check_path: "/health"

alb:
  enabled: true
  use_existing_alb: true
